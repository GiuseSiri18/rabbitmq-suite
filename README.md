# rabbitmq-suite
This project is a high-performance Distributed Task Processing System built on an Event-Driven Architecture (EDA). It demonstrates how to handle asynchronous background jobs by decoupling the request entry point (API) from the processing logic (Workers) using RabbitMQ as a message broker.
